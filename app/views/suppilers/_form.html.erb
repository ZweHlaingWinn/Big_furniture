<%= form_with(model: suppiler, local: true) do |form| %>
  <% if suppiler.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(suppiler.errors.count, "error") %> prohibited this suppiler from being saved:</h2>

      <ul>
      <% suppiler.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :suppiler_name %>
    <%= form.text_field :suppiler_name %>
  </div>

  <div class="field">
    <%= form.label :phone %>
    <%= form.text_field :phone %>
  </div>

  <div class="field">
    <%= form.label :email %>
    <%= form.text_field :email %>
  </div>

  <div class="field">
    <%= form.label :address %>
    <%= form.text_area :address %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
