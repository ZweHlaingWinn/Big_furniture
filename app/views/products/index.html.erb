
<div class="container ">
<div class="row ">
<% @products.each do |product| %> 
  <div class="col-md-4 pt-4 mb-4">
  <%= link_to product_path(product), :action => 'go', class:"no-underline" do %>
    <div class="card card1 showcase">
    <div class=" text-center pt-2"><%= image_tag (product.image), class: "product"%></div>
<div class="card-body">
  <h5 class="card-title card-title1 text-center">
  <%= product.name %>
  </h5>


</div>
  
  </div>
<% end %>
</div>
<% end %>

</div>
</div>

